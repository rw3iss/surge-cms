
We need to make some large modifications to the Admin page, in order to allow for the ability to customize homepage hero items, in the hero banner.
We will design a custom "carousel" output component, that is smart enough to render only a single media item, or a series of items if given, in a carousel.
Please go over all of the below requirements, and create a more in-depth and comprehensive plan to implement all of the desribed features.
Ensure all features below are integrated and added to the plan. If you see a way to improve any feature, or additional features which should definitely be added, you can add or modify it as needed, but keep all of the existing requirements.
Write the implementation plan in
In the admin portal Settings page:
In the general section, I would the ability for the admin to customize the homepage banner image, and text.
This should allow for 'carousel' items. Each item should be able to specify a background image or video, which should directly uploadable from the carousel settings editor, or otherwise be able to select a media from the existing media library (like the Image Block does).

In the Settings page, add a new section for "Home Page" that is collapsable, under the General section, and let's add some homepage customization options.
At first, in the Home Page settings section, build a "carousel" editor. Title the section with "Hero Content". In the beginning of it, show a horizontally scrollable list, where the user can click "card" buttons to "add" new content items (to a list of "carousel" content items). Each of these cards should be kind of large (ie. 1/4 of the width, 4 items, then scrollable), so they can see a large image, and also be able to add text content and show it in these card items.

When the user clicks "Add New Item" replace the block with an 'Edit View' for an empty content item. The content items will allow the user to select from an existing media image or video, or upload a new one, similar to the 'Image Block' on the page details content block editor. When clicking 'Add New Item', show two buttons: 'Select Existing Media', and under it 'Upload New Media'. If the user clicks 'select existing media', show a modal popup where the user can page through existing media, and search for it by title or description in a search bar at the top. Make this new 'Media select' modal a new custom component that is completely isolated, so it can be re-used in multiple areas. The modal should take a filter of "media types" to show in it, and it should then query the backend for all media items of that type. The modal should show a toolbar at the top with two options: a search input bar to search and filter by title and description, and then next to it a sort dropdown, to sort the media items by: date added, date updated, name, size, type.

When the user enters a search, or changes the sort option, the modal should be able to utilize a middle layer service to query the backend for the media items according to those new search parameters.

The initial query, when opening this modal from somewhere, should take an array of types, and sort by 'date updated' by default, for the media items. If an array of types to filter by in the modal is not provider, the modal should query for ALL media types, and show them in all in pages.

As the user types in the search field, it should search and update the shown results, by that query and the sort param and given media types, on a 300ms throttled automatic search.

The modal should show all of the media results, from the search results, in pages of 10 (make the page number easily configurable in the code).

Ensure the "/media" API endpoint can support filtering by multiple media types, and a search query, and sort by a provided sort parameter.

Each shown result should show a preview of the media item (ie. an image, or a video frame for the video). The user should be able to play the media videos in the list results directly, so if they click play on the video, try to load and play it (but you don't need to load all videos at first). Under the item previous, show it's name (and link to that result item's media page), and date updated, and a button to 'Select' it as the content media item choice for whatever source is using the modal (is. use a callback to pass the selected media item back to the source component).

When you have the modular "Select Media" modal working, integrate for use into this new Homepage "Hero Content" section, in the "New Block", when "Add Existing Media" buttons are clicked. After a user selects a media item in the modal, add a new carousel item to the entries of homepage carousel items, with that media item, and show its previous (and ability to play videos) as a new "hero content" block in the horizontal list of items. Each added or existing hero content item in the horizontal list should show as a card, with it's preview at the top, and then some options for the hero item under the preview, for each entry item:
- Image or Video preview at the top of the card.
- A dropdown under it to select the media "layout" type, which should be the css property for "object-fit", ie. 'contain', 'cover', 'fill', etc.
- A button under the layout dropdown to 'Add Header'. When clicked, replace the button with a 2-line textarea, and let the user enter text. Also show after the textarea a vertical list of button options: the top button a small dropdown to select the header size (select from options 'h1' through 'h6'), and then under it a small option button to 'remove' that header entirely, which should remove it and show the "Add header" button again, which should clear all header options for the item. Underneath of the header text entry, also show a custom hex input, to optionally define a text override color. This hex entry should also include a custom 'color picker' component to its right, which when clicked, will open a lightweight color picker modal, and let the user select what color they want. Once selected a color, it should update the hex field with that value, and show a preview of it in the color picker button swatch there. Make a custom 'color picker' component, which can show the button to open the picker, and render the currently selected color as the background of the 'open picker' button, all in one. You can also include the option for this custo component to show the 'hex value' input to the left of the color picker, as an optional prop, if it makes it easier to do that all in one. Store the hex value on the header configuration, along with its size, when saved. The user does not need to enter a hex value. By default it should default to white/#fffff. If no value is entered, or is an error, ensure it falls back to white. The hex input field can show a red border if it's an invalid hex color, though.
- A button under the header button to 'Add Sub-header'. When clicked, replace the button with a 2-line textarea, and let the user enter text for the subheader. Also show after the textarea a vertical list of button options, just the same as the header options above: the top button a small dropdown to select the text sub-header size (select from options 'h1' through 'h6'), and then under it a small option button to 'remove' that sub-header entirely, which should remove it and show the "Add Sub-header" button again. Just like the Header option, also show the hex input and color picker below the sub-header textarea, so the user can customize a color for the sub-header text, and store it with the sub-header text and size configuration.
- A button under the header buttons to 'Add Action'. When clicked, show an input text field for the button label, and also below it an input text area for the url the button will go to when clicked. Also expose an option for 'open in new tab' as a styled toggle button.

In the hero content card items, if the users clicks 'Add New Media', show another modal with a small form to "Add New Media". Make this also a re-usable component, as we will use it in other parts of the site for users to randomly add new media to places, and this modal component should handle uploading the file, showing progress, and letting the user define the title and description for the media. The modal should not be too small, as it will be used to show previews of larger images and videos.

When the user clicks 'Add new Media' from the hero card items, show this new modal component, with a "Select File" button (a custom button, hiding the native file input element), where they can choose a local file to upload. After selecting a file, show a preview of the selected file in a nice way in the center of the modal, with it's basic details below (ie. name, size, type). If it is a video, allow the user to play the video there in the preview.
Under the uploaded file preview section, when a file is selected, show a full width text input for an optional title, and then under it a full width 2-line textarea for its optional description.

At the bottom of the modal show the options to 'cancel' or 'Upload'. When uploading, grey out the options, and show a message above them saying 'Uploading, please wait...' with a loading spinner.
This 'Add New Media' modal should communicate with the client service to upload a new media item to the backend media library, using those provided settings in the modal (ie. upload the actual file to the library, find its reference, and set its title and description to what was provided). Once the media has been added to the library in this new modal, show a 'Media uploaded!' message in place of the 'please wait' message, and then close the modal, and use that new media item, from the response, to add that media as the media item reference to the current hero content item they added it to. In either case of clicking 'add existing media' or 'add new media', the result of both calls should be a reference to a media library item, and it should then show it's preview in the hero content horizontal list when added. In each item in this list, the user can then customize the header and sub-header, for that item, if they want. If the user added a 'video' type of media item, we also need to expose an option for it to 'autoplay' the video, or not. This option should only show if a 'video' media library item is selected.

Each "Hero Content" item, in this horizontal list, should also be draggable, and the user should be able to resort the items horizontally in that list, and the json for the list of items should update the entry positions accordingly, so it can be persisted in that order in the backend.

In the top right of the homepage hero content editor area, show a 'Save' button, which should become enabled when the user makes any changes to this homepage hero content section (or any of the content items). If the user tries to navigate away with changes, prompt to confirm if they want to lose changes.
When the user clicks 'Save' for the homepage hero content, compile all of the current hero content item entries (including their media references, optional headers and subheaders, etc), as a JSON array of entries, and use that JSON blob as the backend data to be stored in the settings. This data should be retrieved from the DB, when requested, and cached to it will be a quick response when querying for the homepage content. Ensure the site is utilizing a caching layer in a clean and proper way (ie. with redis). You can set the backend up to utilize a locally running redis server (which should be running now, in docker or not), if it needs it.

Ensure the homepage carousel item settings can be stored in the database for the site settings (you can make a new settings for "Homepage Hero Items" and store the json configuration there, and cache it on the backend when requested).

Underneath of the 'Homepage' "Hero Content" section, after the horizontal list of homepage carousel items that the user can manage, show a large full-width preview of the homepage hero, with the carousel items rendered. This preview should update and show whenever the user makes a change to the above hero items (but doesn't need to 'save' them yet). So ensure that changing any fields in the carousel items (ie. media item, header input text, or subheader input text) will automatically recompile the output json array of item data, and show it in the preview below the hero content items list.
There should also be a section for global carousel options, above the preview output, and below the hero item list, that allows the user to select to ie. "Automatically scroll hero items".
If the user enables that custom toggle, show an input to the right to define the number of milliseconds to pause on each carousel item, defaulting to 3000 at first.
Also show another styled toggle option for "Repeat", with a sub-message after that explains enabling it will start over from the beginning when the items have finished.
Disable those options if the hero content items only includes 0 or a single content item entry, as they only are applicable if there are multiple hero content item entries for the carousel behavior.
There shoud also be another configurable option, to set the global homepage carousel height. Expose a toggle button here that says "Custom Hero Height", and explains with a message that enabling it will let the user define what relative height they want the carousel to be, in any specification (ie. px, vw, percent, etc). When enabed, show an input text field for the user to enter the height. Make this custom field only accept inputs of the format "<int>px", "<int>vw", "<int>vh", and "<int>%". It should parse out the entry and ensure it adheres to that format, or otherwise show an error. By default this custom height should be 50vh. If the user sets a custom height, make sure it's saved with the hero content global settings here with the rest.

You can save these global hero carousel options with the main "hero content" settings, as one large json blob of the settings, and cache and pull it for the frontend to use as needed. If the user does not have this option enabled, the input should not show, and the homepage hero carousel should not automatically scroll (the user will have to manually page through it).

The large preview under the options should show if there is at least one hero item entered (otherwise you can show the 'Preview' header, but with just a short message after saying the user needs to add items to see a preview). If there is only one item, just show it's media item.
Each rendered hero content item in the preview outside should render at a height relative to what was provided in the above configuration, if given, or otherwise fallback to its default height of '50vh'. However, for the preview output, this value should be scaled down to be relative to the size of the preview component here itself. So normally each item would render at '50vh' on the homepage, or whatever size was configured, but here in the preview, we need to scale this to be relative to the size of the preview. So to do this, calculate the preview components current width, relative to the window's entire width, and get the percentage. Then, multiple the configured hero content 'custom height' value with this relative percentage. So if the preview output is '50%' of the window width, then the height should scale by 50% too. So... render the hero items normally, but at the custom scaled height.
In each rendered content item, render the media item in a background layer for itself, according the the configured 'layout' option on that hero item entry, when it was configured (ie. 'cover', 'contain', etc). Show the media item on the background layer for that carousel item according to its layout. If the media item's type is a 'video', then render the video as the background instead of an image, and check if the option to 'autoplay' the video was set on that hero item. If it was, try to autoplay the video when it comes into view in the carousel. Autoplaying videos should pause after they leave the carousel (ie. after some delay so it isn't paused while animating outward). Then, if that current preview hero item has a 'header' or 'sub-header' entry for itself, then also show those as a layer of text on top of the media item. If a 'header' is defined, show it as large text wrapped in whatever header size they chose in the hero item entry (ie. h1 through h6), and whatever color was set for the header text. Just the same, if there was a 'sub-header' entry for the content item, also render that in the configured size (h1-h6) and hex color, underneath of the 'header' text, if one existed. This preview area should render each hero content item, its custom media as its background, and the optional header and sub-header text on top, in a nice clean fashion, so it will look appropriate in as many cases of text or media configurations as possible (ie. unknown). So ensure the header and subheader elements also have some padding on their sides, so they will not touch when the screen is responsive, and also possibly make the text itself scale with the browser window size, within the hero, as the user resizes the window, so it will still maintain its relative size, layout, and look, no matter what the hero size. Try to make the text look nice and responsive, but start at the configured base "h1-h6" sizes. For the preview output, we probably need to scale the headers down a little more, relative to the size of the preview, so ensure they scale with the PREVIEW components sizing or dimensions, and not the page, in that case. Finally, after rendering the optional headers for the item, also see if there a was an 'Action button' enabled for that item as well, and show that underneath the header texts, with a bit of a gap above it. All hero content should be centered vertically and horizontally within their containers.

If there is more than one hero content item configured in the preview here, ensure the carousel will be setup, and show the 'left' and 'right' arrow buttons to page through it. If the user enabled 'automatic scrolling' option for the hero items, then setup the interval to automatically page through the hero items in the preview, at the configured page delay time. At the end of the carousel, if the user enabled the 'repeat' option, cycle back to the beginning (it should be an infinite/seamless change if possible, not scrolling "left" through all the items).
Ensure that if the user changes the automatic scroll option time above, the old interval or timeout is remove, and a new one created with the changed interval time.

Find a lightweight universal carousel solution that will work for this, that is responsive, and supports touch scrolling, etc, and infinite scroll. It needs to work with SolidJS. You can build a custom component if you want, it might not be that hard. We just need to it automatically scroll through the items.

This preview area can be the exact same custom "hero carousel" component that the homepage will use, and it should just handle receiving an array of the hero items, and render them in a carousel, as described. This component should be used on the preview section here in the admin, in a slightly smaller view, and on the homepage itself, in full view. Ensure it is composable, re-usabe, and modular.

Once you have the hero content preview and carousel working nicely in the admin, as a custom component, ensure it saves the configuration in the backend, and then... integrate the new custom hero carousel component the existing homepage, in full width, at the top below the site header. Set the height to whatever the configured custom hero height was in the settings, explained above, if one was provided, otherwise use the default height of '50vh'.
Just like the preview element, since this should use the same shareable component anyway, ensure the live rendered homepage output include the carousel arrows, if there is more than one item, and ensure it is setup to automatically scroll and repeat, if those options were enabled.